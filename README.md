Install the requirements using "pip install -r requirements.txt"
Download the preffered llama cpp gguf model and change the name in lang.py
Run lang.py to create the vector database.
Finally run lang.py to start chatting.
